---
machine:
  services:
    - docker

dependencies:
  pre:
    - sudo pip install docker-compose
  override:
    - docker-compose build:
        pwd: test

test:
  override:
    - docker-compose run tests:
        pwd: test

