---
machine:
  services:
    - docker

dependencies:
  pre:
    - pip install docker-compose
  override:
    - docker-compose build
  pwd: test

test:
  override:
    - docker-compose run tests
  pwd: test

